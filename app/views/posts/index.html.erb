<h1>General Posts</h1>
<br>
<div id = "outer">
<div class = "inner"><%=button_to "Change", change_posts_path, method: :get %></div>
<div class = "inner"><%=button_to "Show All", posts_path, method: :get %></div>
</div>
<br>
<div class="container">
    <div class="row">
        <% @posts.each do |post|%>
            
                <div class="col-md-4">
                    <h2><%= post.title %></h2>                   
                    <%=User.find(post.user_id).username%>                    
                    <br>
                    <%= image_tag(post.image, style: 'width: 50%') %>
                    <br>
                    <%= truncate(post.body, :length => 50)%>
                    <br>
                    <%= button_to "Read Post", post, method: :get %>
                    <br>
                </div>              
            
        <%end %>
    </div>
</div>
<br>
<div >
<%= pagy_bootstrap_nav(@pagy).html_safe%>
</div>