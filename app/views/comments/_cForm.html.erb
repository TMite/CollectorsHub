<%= form_for ([@post,@post.comments.build]) do |f|%>
    <p><%=f.label :email %>
        <%=f.text_field :email %>
    </p>
    <p><%=f.label :body %>
    <%= f.text_area :body %>
    </p>
    <br>
    <%= f.submit class: "btn btn-primary"%>
<%end %>